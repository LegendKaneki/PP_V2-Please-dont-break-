/// @description Insert description here
// You can write your code in this editor

if(keyboard_check(vk_add) && display_width > 640 * global.window_scale)
{
	global.window_scale += .5;
	
	window_set_size(640 * global.window_sc)
}